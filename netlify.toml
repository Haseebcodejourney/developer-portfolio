# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Optional: SPA fallback if you add client-side path routing later
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
